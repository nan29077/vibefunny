// ===========================================================================
// 하위 호환 배럴 — 기존 코드의 import 경로를 바꾸지 않아도 된다.
// 실제 구현은 src/lib/db/ 디렉토리에 있다.
//   - db/json-provider.ts  : JSON 파일 기반 DB (현재 사용 중)
//   - db/index.ts          : DATABASE_TYPE 에 따른 프로바이더 선택
//   - db/types.ts          : 공통 타입 정의
// ===========================================================================

export { getDb, saveDb, tx } from "./db/index";
